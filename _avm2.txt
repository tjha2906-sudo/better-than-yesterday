AT 2026 : rawNetwork);     const algorandClient = this.getAlgorandClient(network);     const assetId = this.getAssetId(asset, network);     const feePayer = extra?.feePayer;     let paymentIndex = 0;     const emptySigner = makeEmptyTransactionSigner();     const composer = algorandClient.newGroup();     if (feePayer) {       composer.addPayment({         sender: feePayer,         receiver: feePayer,         amount: microAlgo(0),         note: `x402-fee-pa
